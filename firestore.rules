rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Jobs collection - allow read/write for now (can be restricted later with auth)
    match /jobs/{jobId} {
      allow read, write: if true;
    }
  }
}

